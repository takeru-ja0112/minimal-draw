アイコンシステムの追加
-
M_USERテーブル配下に以下のようなカラムを追加する。
- icon_name : string // 使用するアイコン名
- icon_color : string // アイコンに使用する色

\\wsl.localhost\Ubuntu-24.04\home\takeru\minimaldraw-02\minimal-draw\utils\Icons.tsの中でアイコンライブラリを追加しており、一覧も追加しています。
具体的に必要な機能しては、TopPage.tsxのユーザー名設定画面にて、アイコン一覧からアイコンとカラーを設定する画面の追加（モーダル式）

カラー候補はカラーピッカーで自由に決められるようにする。

設定されたアイコンは各画面で自由に呼び出せるロジックにする。